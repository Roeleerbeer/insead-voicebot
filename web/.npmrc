# Allow native builds for better-sqlite3 (and sharp, transitively from next).
# pnpm 10+ blocks these by default. Without it the Linux container build fails
# with [ERR_PNPM_IGNORED_BUILDS].
dangerously-allow-all-builds=true
